# SHIP-OS MCP server

Milestone 1 placeholder. This package will expose SHIP-OS as MCP tools
(`shipos.search_orders`, `shipos.get_order`, later mutations) by wrapping the
existing SHIP-OS JSON API (`/api/v1/mobile/*`) with desktop-link token auth.

Design decisions live in ../SPEC.md (Milestone 1: read-only; buy label in M2).
